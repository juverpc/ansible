version: 0.2

env:
  secrets-manager:
    VM_PASSWORD: "TestSecret:vm_linux"

phases:

  install:

    commands:
      - yum -y install ansible
      - yum -y install python-winrm
  
  pre-build:
    commands:
      - chmod +x script.sh
      - ./script.sh
      - cat inventory
      #- ansible-playbook -i inventory playbook.yml
  


