version: 0.2

env:
  secrets-manager:
    VM_PASSWORD: arn:aws:secretsmanager:us-east-1:624733648801:secret:vm_linux-2hdUKB

phases:

  install:

    commands:
      - yum -y install ansible
      - yum -y install python-winrm
  
  pre_build:
    commands:
      - chmod +x script.sh
      - ./script.sh
      - cat inventory.txt
  # build:
      # - ansible-playbook -i inventory playbook.yml
  


