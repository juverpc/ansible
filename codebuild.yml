version: 0.2

env:
  secrets-manager:
    VM_PASSWORD: arn:aws:secretsmanager:${AWS_REGION}:624733648801:secret:vm_linux-2hdUKB
phases:

  install:

    commands:
     
      - pip install boto3
      - pip install botocore
      - pwd
      - yum -y install ansible
      - yum -y install python-winrm
      - ansible-galaxy collection install amazon.aws
      - ansible-playbook aws_host.yml -vvv
      #- echo $VM_PASSWORD[0]

  # pre_build:
  #   commands:
  #     # - chmod +x replace.py
  #     # - python3 replace.py
  #     - cat ./group_vars/tableau_servers.yml
  #     - cat ./json_file.json
  # build:
  #     - ansible-playbook aws_host.yml -vvv
  


